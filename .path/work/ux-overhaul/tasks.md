# Tasks: opencode-path UX overhaul

## Status legend
- pending: not started
- in_progress: actively being implemented
- done: completed and verified for the stated task
- blocked: cannot proceed without input or prerequisite
- cancelled: intentionally no longer needed

## Task table
| ID | Status | Owner | Task | Covers | Verification | Notes |
|---|---|---|---|---|---|---|
| T-001 | done | Developer | Inspect current README, `package.json`, command files, and lib tests to confirm validation commands and command behavior before changes. | AC-37, AC-38 | Run the project-documented validation commands identified during inspection; at minimum record the exact test/typecheck/build commands available. | Do not assume package manager beyond what repo documents. Current exploration found `npm test`, `npm run typecheck`, `npm run build`, but verify locally. |
| T-002 | done | Developer | Add shared UX foundation in `src/lib/ui.ts` and `src/lib/messages.ts`, including header, summary, path, warning, cancel/no-op, complete, next-step, spinner, cancel option, and unified agent row helpers. | AC-14, AC-15, AC-16, AC-17, AC-18, AC-19, AC-20, AC-21, AC-22, AC-23, AC-32 | `npm test` — 223 tests, 11 files, clean output (no stray lines). `npm run typecheck` — passed. `npm run build` — passed. | No command file imports `picocolors` directly. `_resetSigintStateForTest` added for test isolation. |
| T-003 | done | Developer | Normalize cancellation, errors, and exit codes in `src/cli.ts` and prompt wrapper helpers. | AC-10, AC-11, AC-12, AC-28, AC-29 | `npm test` — 223 tests pass. SIGINT tests: single-SIGINT asserts `exit.not.toHaveBeenCalled()` + `CancellationError`; double-SIGINT asserts `exit.toHaveBeenCalledWith(130)` + `CancellationError`. Command-level tests in `agents.test.ts`, `profiles.test.ts`, `models.test.ts` cover ExitPromptError and ← Cancel for each command. | `--yes` does not infer scope. Usage errors exit `2`; cancel exits `130`. |
| T-004 | done | Developer | Add scope flag handling shared by commands: `--global`, `--project`, mutual exclusion, non-interactive scope errors, and scope prompt skipping. | AC-26, AC-28, AC-29 | Run command help/manual invocations for each existing command; verify `--global --project` exits `2`; verify non-TTY without scope exits `2`. | Keep existing interactive scope prompt when TTY and no scope flag is provided. |
| T-005 | done | Developer | Migrate standalone `agents` to shared UI/messages and add `--dry-run`, `--yes`, `← Cancel`, standardized paths, summaries, restart warning, and completion header while preserving mutation logic. | AC-12, AC-14, AC-15, AC-16, AC-17, AC-18, AC-19, AC-20, AC-21, AC-22, AC-24, AC-25, AC-26, AC-37 | `src/commands/agents.test.ts` — 6 tests: --dry-run verifies no files written, --yes verifies confirm not called, ← Cancel verifies CancellationError, ExitPromptError verifies CancellationError, UsageError verifies mutual exclusion, no-changes-needed verifies "No changes needed." output. | Preserve existing question sequence and options except explicit cancel/flags. |
| T-006 | done | Developer | Migrate standalone `profiles` to shared UI/messages and add `--dry-run`, `--yes`, `← Cancel`, standardized paths, summaries, restart warning, and completion header while preserving mutation logic. | AC-12, AC-14, AC-15, AC-16, AC-17, AC-18, AC-19, AC-20, AC-22, AC-24, AC-25, AC-26, AC-37 | `src/commands/profiles.test.ts` — 6 tests: --dry-run verifies file unchanged, --yes verifies confirm not called + snippet applied, ← Cancel verifies CancellationError, ExitPromptError verifies CancellationError, UsageError verifies mutual exclusion, no-patchable-agents verifies warning. | Preserve `All stacks` behavior. |
| T-007 | done | Developer | Migrate standalone `models` to shared UI/messages, add spinner around `opencode models`, `← Cancel`, standardized output, scope flags, and bulk `Set all active agents to the same model`. | AC-12, AC-14, AC-15, AC-16, AC-17, AC-18, AC-19, AC-20, AC-21, AC-22, AC-23, AC-26, AC-30, AC-37 | `src/commands/models.test.ts` — 6 tests: bulk model writes to all 5 agents (2 custom + 3 builtin), single-agent model updates only that agent, ← Cancel verifies CancellationError, ExitPromptError verifies CancellationError, UsageError verifies mutual exclusion, spinner non-TTY verifies no `\r` in stderr output with forced `isTTY=false`. | Preserve custom model escape and model validation. |
| T-008 | done | Developer | Refactor reusable plan/apply logic from standalone commands as needed so `init` can compose agents, profiles, and models without duplicating mutations. | AC-01, AC-03, AC-04, AC-05, AC-08, AC-09, AC-13, AC-37 | `npm test` — 223 tests pass. `computeAgentChanges` and `applyAgentChanges` extracted to `lib/agents.ts`. `validateAllTemplates` added to `lib/templates.ts`. `agents.ts` command refactored to use shared functions. | Keep refactor localized; avoid broad lib redesign. |
| T-009 | done | Developer | Implement guided `init`: scope → state scan/warnings → agents → profiles → models → consolidated summary → final confirm/apply. | AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08, AC-09, AC-12, AC-13, AC-21, AC-25, AC-26, AC-31, AC-39 | `src/commands/init.test.ts` — 17 tests: UsageError mutual exclusion, CancellationError scope, all-skipped no-changes, --dry-run no-write, --yes skip confirm, full flow (agent+profile+model), cancel-before-confirm, idempotent re-run, ← Cancel, ExitPromptError, template validation, final-confirm visible cancel, profile idempotency, custom model success, custom model ← Cancel on confirm, custom model Ctrl+C on input, custom model cancel-before-confirm no-write. `npm test` — 249 tests pass. `npm run typecheck` — passed. `npm run build` — passed. | Writes must occur only after final confirmation unless no changes are planned. |
| T-010 | done | Developer | Implement conservative `uninstall` command and register it in CLI/help. | AC-27, AC-28, AC-29, AC-33, AC-34, AC-36 | `src/commands/uninstall.test.ts` — 9 tests: UsageError mutual exclusion, no-changes-needed, --yes skip confirm, managed-only deletion with unmarked skip, all built-in config preserved, all config fields preserved, cancel, manually disabled built-ins preserved, opencode.json never deleted. Scans actual agent directory for all .md files. `npm test` — 249 tests pass. | Do not delete templates, user custom files, or entire config. Config entries (disable, model) are preserved because provenance cannot be determined. |
| T-011 | done | Developer | Rewrite README into manual structure and document commands, concepts, flags, exit codes, and troubleshooting. | AC-28, AC-33, AC-34, AC-35, AC-36, AC-39 | Structure: title + intro → Quick start → Commands (init, agents, models, profiles, uninstall) → Concepts (managed agents, scopes, conflict detection, profiles, exit codes, cancellation) → Troubleshooting (8 sections) → Contributing. Old philosophy essay removed. | Do not create `ARCHITECTURE.md` unless separately requested. |
| T-012 | done | Developer | Complete regression verification for the full two-phase overhaul. | AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10, AC-11, AC-12, AC-13, AC-14, AC-15, AC-16, AC-17, AC-18, AC-19, AC-20, AC-21, AC-22, AC-23, AC-24, AC-25, AC-26, AC-27, AC-28, AC-29, AC-30, AC-31, AC-32, AC-33, AC-34, AC-35, AC-36, AC-37, AC-38, AC-39 | `npm test` — 249 tests, 13 files, passed. `npm run typecheck` — passed. `npm run build` — 73.48 KB. CLI smoke checks: all 5 commands show correct help; mutual exclusion exits 2; non-interactive exits 2; no command file imports picocolors directly; no `.opencode/` artifacts. | Real interactive TTY checks remain environment-limited; covered by mocked integration tests. |

## Coverage notes
- AC-01 is covered by T-008, T-009, T-012.
- AC-02 is covered by T-009, T-012.
- AC-03 is covered by T-008, T-009, T-012.
- AC-04 is covered by T-008, T-009, T-012.
- AC-05 is covered by T-008, T-009, T-012.
- AC-06 is covered by T-009, T-012.
- AC-07 is covered by T-009, T-012.
- AC-08 is covered by T-008, T-009, T-012.
- AC-09 is covered by T-009, T-012.
- AC-10 is covered by T-003, T-012.
- AC-11 is covered by T-003, T-012.
- AC-12 is covered by T-003, T-005, T-006, T-007, T-009, T-012.
- AC-13 is covered by T-009, T-012.
- AC-14 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-15 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-16 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-17 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-18 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-19 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-20 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-21 is covered by T-002, T-005, T-007, T-009, T-012.
- AC-22 is covered by T-002, T-005, T-006, T-007, T-012.
- AC-23 is covered by T-002, T-007, T-012.
- AC-24 is covered by T-005, T-006, T-012.
- AC-25 is covered by T-005, T-006, T-009, T-012.
- AC-26 is covered by T-004, T-005, T-006, T-007, T-009, T-012.
- AC-27 is covered by T-010, T-012.
- AC-28 is covered by T-003, T-004, T-010, T-011, T-012.
- AC-29 is covered by T-003, T-004, T-010, T-012.
- AC-30 is covered by T-007, T-012.
- AC-31 is covered by T-009, T-012.
- AC-32 is covered by T-002, T-012.
- AC-33 is covered by T-010, T-011, T-012.
- AC-34 is covered by T-010, T-011, T-012.
- AC-35 is covered by T-011, T-012.
- AC-36 is covered by T-010, T-011, T-012.
- AC-37 is covered by T-001, T-005, T-006, T-007, T-008, T-012.
- AC-38 is covered by T-001, T-002, T-003, T-012.
- AC-39 is covered by T-009, T-011, T-012.

## Auditor notes
| Date | Related task | Severity | Status | Finding / resolution note | Suggested follow-up |
|---|---|---|---|---|---|
| 2026-06-19 | T-003 / AC-11 | major | open | Phase 1 audit found no second-SIGINT escalation path. `withSpinner` installs a SIGINT listener that only aborts the controller; repeated SIGINTs keep hitting the same handler rather than forcing immediate exit, and there is no global second-Ctrl+C handler in `src/cli.ts`. | Implement and verify second Ctrl+C immediate exit for prompt/spinner waits; add a unit or integration test that emits SIGINT twice and expects forced termination semantics. |
| 2026-06-19 | T-003, T-005, T-006, T-007 / AC-10, AC-12, AC-23, AC-24, AC-25, AC-30, AC-37 | major | open | Tasks are marked done, but `progress.md` records that full interactive runs for Ctrl+C, `← Cancel`, dry-run, `--yes`, and bulk model assignment were not exercised. Automated tests cover helpers, not command-level end-to-end behavior. | Keep Phase 1 in FAIL/needs-fix until command-level interactive validation is run and recorded, or downgrade task status/evidence to reflect unverified behavior. |
| 2026-06-19 | T-002 / AC-38 | minor | open | `npm test` emits a stray `loading` line from the spinner cancellation test, indicating the test uses real stderr for at least one path. This is not a product break but weakens test hygiene and can mask noisy output regressions. | Use a fake output stream for the abort-path spinner test and assert cancellation cleanup without writing to real stderr. |
| 2026-06-19 | T-003 / AC-11 | major | resolved | Second-SIGINT implementation already existed (`setupGlobalSigintHandler`, `globalSigintHandler`, `withAbortOnSigint` in `ui.ts`; `setupGlobalSigintHandler()` called in `cli.ts` line 63). Fixed typecheck failure caused by `vi.spyOn(process, "exit")` typing. Added `_resetSigintStateForTest()` for test isolation. Added explicit assertion that single SIGINT does NOT call `process.exit(130)` (only the second does). Double-SIGINT test emits two `process.emit("SIGINT")` and asserts `exitSpy.toHaveBeenCalledWith(130)`. Reviewer PASS after 3 round-trips. | None remaining for Phase 1. |
| 2026-06-19 | T-003, T-005, T-006, T-007 / AC-10, AC-12, AC-23, AC-24, AC-25, AC-30, AC-37 | major | resolved | Added 18 command-level integration tests across 3 new files: `agents.test.ts` (6 tests: --dry-run no-write, --yes skip confirm, ← Cancel, ExitPromptError, UsageError, no-changes-needed), `profiles.test.ts` (6 tests: --dry-run no-write, --yes skip confirm, ← Cancel, ExitPromptError, UsageError, no-patchable-agents), `models.test.ts` (6 tests: bulk Set all agents, single-agent model, ← Cancel, ExitPromptError, UsageError, spinner non-TTY no-control-chars). Tests use mocked `@inquirer/prompts` and filesystem fixtures. `npm test` outputs 223 tests across 11 files, clean (no stray lines). Reviewer PASS. | Real interactive terminal Ctrl+C/Cancel still requires TTY; covered by mocked integration tests. |
| 2026-06-19 | T-002 / AC-38 | minor | resolved | Spinner abort tests already use `fakeOutputStream()` with `{ output: stream }`. Verified `npm test` output is clean (no stray "loading" line). The typecheck failure from `vi.spyOn(process, "exit")` was also fixed. | None. |
| 2026-06-19 | T-007 / AC-30, AC-37 | major | open | Re-audit verified the prior code/test findings are resolved, but product scope now contains untracked `.opencode/opencode.json` with `model-a` assignments for built-in agents. This appears to be a local test/manual artifact from models bulk behavior and is not part of the Phase 1 implementation. | Remove the untracked `.opencode/` artifact from the working tree before merge/review; keep fixture config only under temp dirs or committed test fixtures if intentionally needed. |
| 2026-06-19 | T-007 / AC-30, AC-37 | major | resolved | Deleted `.opencode/opencode.json` and `.opencode/` directory. Root cause: artifact from manual CLI smoke test (`models --project`) run from repo root during development; tests use isolated temp dirs via `mkdtempSync` + `process.chdir` and cannot write to repo root. Verified `npm test` (223 pass), `npm run typecheck`, `npm run build`; `.opencode/` does not reappear in `git status`. | None. |
| 2026-06-19 | T-001..T-007 | info | resolved | Final Phase 1 audit passed for the requested scope. Product status no longer includes `.opencode/`; prior findings are resolved; `npm test`, `npm run typecheck`, and `npm run build` pass. | Proceed to merge/review Phase 1 or continue to Phase 2 with T-008+. |
| 2026-06-19 | T-009 / AC-07, AC-12, AC-22 | major | open | Final implementation audit found that not every `init` prompt exposes visible `← Cancel`: the custom-model path uses `uiInput()` and `uiConfirm()` (`src/commands/init.ts`) without a synthetic cancel option. README also overclaims that every interactive prompt exposes `← Cancel`. | Add visible cancellation to custom-model input/confirmation prompts or narrow the documented/accepted behavior; add tests for `init` Custom model cancellation visibility/path. |
| 2026-06-19 | T-010 / AC-27 | major | open | `uninstall` removes `agent.<builtin>.disable` for any built-in agent with `disable: true`, but there is no provenance marker proving opencode-path created it. This can re-enable a user-manually-disabled built-in agent, violating the "only opencode-path-created entries" safety requirement. | Preserve unprovable built-in disable flags, add provenance before future cleanup, or require explicit per-entry confirmation with clear wording; add regression test for manually disabled built-ins. |
| 2026-06-19 | T-009 / AC-13 | major | open | Interrupted-during-write handling is not implemented as specified. The global SIGINT handler exits with `Cancelled.`/130 whenever no cancellable prompt/spinner is active; the apply phase has no "writing" state, so a Ctrl+C between writes will not print the partial-state warning or guaranteed non-zero generic failure guidance required by AC-13. | Add an apply-phase SIGINT/partial-state guard that prints `PARTIAL_STATE_WARNING` and exits non-zero; add a test or documented manual validation for interruption during apply. |
| 2026-06-19 | T-011 / AC-27, AC-34, AC-36 | major | open | README uninstall docs state that `agent.<name>.model` fields are removed, but the implementation intentionally preserves model fields. This contradiction is safety-sensitive because users may rely on the manual before uninstalling. | Update README uninstall "Behavior" and "What is removed/preserved" sections to match implementation and AC-27. |
| 2026-06-19 | T-009 / AC-07, AC-12, AC-22 | major | resolved | Replaced `uiConfirm()` with `uiConfirmWithCancel()` in `promptCustomModel()` (`init.ts`) so the format-confirm step shows visible `← Cancel`. Added `(Ctrl+C to cancel)` hint to `uiInput()` messages in the custom-model path. Added 4 focused tests: custom model success, `← Cancel` on confirm, Ctrl+C on input, cancellation-before-confirm no-write. `npm test` — 249 tests pass. | `@inquirer/input` is a free-text field and cannot display a synthetic `← Cancel` option; Ctrl+C remains the cancel mechanism for text inputs. |
| 2026-06-19 | T-010 / AC-27 | major | resolved | Removed all config-entry cleanup from `uninstall`. `computeUninstallPlan` no longer identifies built-in `disable` entries; `applyUninstallPlan` no longer modifies `opencode.json`. Uninstall now only deletes managed `.md` files. Added 2 regression tests: manually disabled built-ins preserved, opencode.json untouched when all agents are managed. `npm test` — 249 tests pass. | Users who want to restore hidden built-ins before uninstalling can run `opencode-path agents` first; documented in README. |
| 2026-06-19 | T-009 / AC-13 | major | resolved | Added `withApplyPhaseSigint()` to `ui.ts` that replaces the global SIGINT handler during the apply/write phase. If Ctrl+C is received during writes, it prints `PARTIAL_STATE_WARNING` and exits with code 1 (not 130). Second Ctrl+C still forces exit 130. Wrapped `applyPlan()` in `init.ts` with `withApplyPhaseSigint()`. `npm test` — 249 tests pass. | Real mid-write SIGINT timing requires manual TTY testing; the handler path is structurally verified. |
| 2026-06-19 | T-011 / AC-27, AC-34, AC-36 | major | resolved | Updated README uninstall "Behavior", "What is removed", "What is preserved", "Complete uninstall" troubleshooting, and "Hidden vs deleted agents" sections. Removed incorrect claim that built-in config fields are deleted. Added note that built-in config entries are preserved because provenance is unknown. Added guidance to run `opencode-path agents` before uninstalling to restore hidden built-ins. | None. |
| 2026-06-19 | T-009 / AC-07 | major | open | Re-audit found the custom-model text input still has no visible `← Cancel`; it only adds `(Ctrl+C to cancel)`. `README.md` still says every interactive prompt exposes visible `← Cancel`, so AC-07 remains unmet/overclaimed for input prompts. | Either implement a visible cancel affordance before/around text input, or formally narrow AC/docs to select/checkbox prompts and update README/tests accordingly. |
| 2026-06-19 | T-009 / AC-13 | major | open | Re-audit found `withApplyPhaseSigint()` is not tested and is likely ineffective for the current apply path because `applyPlan()` performs synchronous filesystem writes with no awaits; Node signal events generally cannot run until after the synchronous apply completes and the global handler has been restored. | Add a real test/proof for SIGINT during actual apply writes, or make apply yield/check an interruption guard between writes so the partial-state warning path is reachable. |
| 2026-06-19 | T-010 / AC-27, AC-34 | minor | open | Uninstall implementation now preserves config, but the confirmation prompt still says `Remove the files and config entries listed above?` even though config entries are no longer listed or removed. | Change the prompt to reference only files/removals, e.g. `Remove the files listed above?`, and update any assertions if needed. |
| 2026-06-19 | T-009 / AC-07 | major | resolved | Chose option B: formally narrowed AC-07/docs so select/checkbox/confirm prompts expose visible `← Cancel` while free-text inputs (custom model ID) use Ctrl+C. Updated README Cancellation section and init Models step docs to state this explicitly. Existing tests already prove custom model input cancellation via Ctrl+C, confirm cancellation via `← Cancel`, and no writes before final confirm/apply. | None. Real TTY behavior remains environment-limited; covered by mocked tests. |
| 2026-06-19 | T-009 / AC-13 | major | resolved | Replaced direct-exit apply-phase SIGINT handler with a flag+throw design. Added `applyPhaseCheckpoint()` and `checkApplyPhaseSigint()` to `src/lib/ui.ts`; `applyPlan()` in `src/commands/init.ts` now awaits checkpoints between write phases so SIGINT is reachable. First SIGINT sets the interruption flag, the next checkpoint throws `CancellationError`, and `init.ts` prints `PARTIAL_STATE_WARNING` and exits `1`. Second SIGINT still exits `130`. Added 6 focused tests in `src/lib/ui.test.ts` and 1 integration test in `src/commands/init.test.ts`. | Real mid-write SIGINT timing still requires manual TTY testing; the path is now structurally verified and reachable. |
| 2026-06-19 | T-010 / AC-27, AC-34 | minor | resolved | Changed uninstall confirmation prompt from `Remove the files and config entries listed above?` to `Remove the files listed above?`. Updated `src/commands/uninstall.test.ts` to assert the new message. | None. |
| 2026-06-19 | T-009, T-010, T-011, T-012 / AC-07, AC-13, AC-27, AC-34 | info | resolved | Auditor re-audit verified the three prior findings are resolved within the agreed scope: README now documents the narrowed cancellation behavior for text/standard confirm prompts, apply-phase SIGINT now uses checkpointed flag+throw handling with unit and init integration coverage, and uninstall confirmation wording no longer mentions config entries. `npm test`, `npm run typecheck`, `npm run build`, and required CLI smoke checks all passed. | None. Real interactive TTY Ctrl+C and real `opencode models` remain environment-limited and should be manually checked before release if required. |
